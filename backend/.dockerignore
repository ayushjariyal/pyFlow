# Keep the build context small and avoid copying host-only state into the image.
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/

# Local SQLite DB and uploaded/generated files — these live in a Docker volume.
*.db
storage/
_test_storage/

.env
