# Pinned to versions that ship prebuilt wheels for Python 3.12 through 3.14,
# so `pip install` never needs a compiler.
#
# Web framework
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.20  # required by FastAPI for file uploads (CSV)

# Database (SQLite is built into Python — no driver needed)
sqlalchemy==2.0.50
alembic==1.18.4           # Database migrations

# Settings / validation
pydantic==2.13.4
pydantic-settings==2.14.1

# Async task queue (Phase 2)
celery==5.6.3             # distributed task queue / worker
redis==8.0.0              # Redis client — Celery broker + result backend

# Data processing (Phase 5)
pandas==3.0.3             # CSV analysis / cleaning / profiling / conversion
numpy==2.4.6              # pandas dependency (pinned for reproducibility)
openpyxl==3.1.5           # XLSX output for FILE_CONVERSION

# Testing
pytest==8.4.2
httpx==0.28.1             # used by FastAPI's TestClient

# --- PostgreSQL driver ---
# Used in deployment (Render/Railway/etc.). Harmless locally — SQLite is still
# the default unless DATABASE_URL points at Postgres.
psycopg2-binary==2.9.10
