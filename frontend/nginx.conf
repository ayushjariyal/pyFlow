server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: unknown paths (e.g. /workflows/123) fall back to index.html
    # so React Router can handle them client-side.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
