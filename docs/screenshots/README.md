# Screenshots

Drop the images referenced by the main README here:

| File | Page to capture |
|------|-----------------|
| `dashboard.png` | The Dashboard (job submission form + recent jobs) |
| `jobs.png` | The Jobs list / a job's detail view with a result |
| `workflow.png` | A workflow detail page showing the DAG graph |
| `metrics.png` | The workflow metrics view |

PNG or GIF both work. Keep them reasonably sized (~1200px wide) so the README
loads quickly on GitHub.
